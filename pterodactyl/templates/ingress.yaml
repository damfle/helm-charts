{{- if .Values.panel.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "pterodactyl.fullname" . }}
  labels:
    {{- include "pterodactyl.labels" . | nindent 4 }}
  {{- with .Values.panel.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.panel.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.panel.ingress.tls }}
  tls:
    {{- range .Values.panel.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.panel.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "pterodactyl.fullname" $ }}
                port:
                  number: {{ $.Values.panel.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
